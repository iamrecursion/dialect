import SwiftUI

/// Placeholder shell.
struct ContentView: View {
    var body: some View {
        VStack(spacing: 4) {
            Text("λ").font(.largeTitle)
            Text("Dialect").font(.headline)
        }
    }
}

#Preview {
    ContentView()
}
